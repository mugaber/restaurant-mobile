import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
})

const NavHeaderStyles = {
  headerStyle: {
    backgroundColor: '#333',
  },
  headerTintColor: '#fff',
}

export { styles, NavHeaderStyles }
